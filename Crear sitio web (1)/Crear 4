# Cambia 'nune-site' por el nombre que quieras usar en fly (o usa `fly launch` para generarlo)
app = "nune-site"
primary_region = "iad"

[build]
  # Se usará el Dockerfile que añadimos en esta carpeta
  dockerfile = "Dockerfile"

[env]

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    port = 80
    handlers = ["http"]

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]
